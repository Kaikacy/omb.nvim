!! experimental !!
- FULL REWRITE; REMOVE COMPONENTS
    * Simplifies plugin and selector creation, improving DX and UX *
    Component objects and ID system will be removed. Instead config will be passed to selector creation function.
    Selector creation function will return selector object with methods.
    Source, display and handler options will still be distinguishable for easy config sharing between selectors.
    Conciderations:
        - Huge rewrite; API and backend will be redesigned.
